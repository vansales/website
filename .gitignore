/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
*.log
.DS_Store

# local env (secrets) — keep .env.example committed
.env
.env*.local

# internal planning notes — local only, not published
/docs/
